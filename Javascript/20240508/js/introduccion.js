//
// VARIABLES *********************************************************
//
var nombreVariable; // De este modo definimos una variable llamada nombreVariable
var otraVariable = 5; // Definimos la variable otraVariable y le asignamos el valor 5
nombreVariable = 9;
nombreVariable = 7;
// let -> es otro modo de definir variables

//
// COMENTARIOS *********************************************************
//

// Esto es un comentario de una línea

/*
Esto es un comentario
que puede abarcar
diferentes líneas
*/

//
// CONSTANTES *********************************************************
//
const nombreConstante = 3.1416; // De este modo creamos una constante llamada nombreConstante de valor 3.1416

// nombreConstante = 5.5555; A una constante no se le puede cambiar el valor

//
// COMO VER DATOS EN CONSOLA *********************************************************
//
console.log( nombreVariable ); // Se escribe en el terminal el valor de nombreVariable

console.log( "La variable nombreVariable vale: " + nombreVariable ); // Se escribe en el terminal el valor de concatenar ese texto con la variable

console.log( otraVariable );

console.log( nombreConstante );

//
// TIPOS DE DATOS *********************************************************
//
var entero = 5; // un tipo de datos son los números enteros
var otroEntero = -25; // los enteros también pueden ser negativos

var real = 3.1416; // un tipo de datos son los números reales. Usaremos el punto como separador decimal
var otroReal = -9.81; // también pueden ser negativos

var cadenaDeTexto = "Hola don Pepito, hola don José"; // Las cadenas de texto van dentro de comillas dobles

var booleanos = true; // Definimos una variable booleana que vale verdad
var otroBooleano = false; //Para algo falso usaremos la palabra reservada false

var lista = []; // Para definir arrays usamos los corchetes
var otraLista = [3, 4, 5, 6, 9]; //Definimos una lista de 5 enteros
var otraOtraLista = [3, "Hola", true, false, 4, 3.1416, 3.2325]; // También podemos definir listas heterogéneas

var matriz = [ [1, 2],["Hola", true, 1, 1] ]; //Dentro de una lista, puede haber listas


